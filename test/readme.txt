The first unit test is testing whether or not data is being correctly bound to the testRating object.

The second unit test is testing whether or not data is being correctly bound to the tripInfo object.

Both of these objects are used in our database queries so proper binding of data sent from http requests is a must.